# AgentProbe

The trust and discovery layer for the MCP economy — find agent tools by what they do, and verify the identities and reputation behind every endpoint (MCP, A2A, x402, ERC-8004).

🌐 [agentprobe.xyz](https://agentprobe.xyz)

```mermaid
flowchart LR
  URL["Agent endpoint"] --> AP["AgentProbe · assessment pipeline"]
  AP --> VAL["Protocol validators<br/>MCP · A2A · x402 · ERC-8004 · UCP"]
  VAL --> TRUST["Trust model<br/>Verified · Plausible · Unverified"]
  TRUST --> DIR["Directory<br/>discover · verify"]
```

## Core

{{core}}

- **mcp-agent-assessment**: Unified 4-layer assessment pipeline that orchestrates all protocol validators into a single score

## Protocol Validators

{{protocols}}

- **a2a-agent-validator**: A2A Protocol agent card discovery and AP2 extension detection
- **x402-mcp-validator**: x402 payment protocol validation — probes all four schemes (exact, upto, batch-settlement, auth-capture) and MCP connection/version-branch capabilities
- **erc8004-registry-parser**: ERC-8004 on-chain agent registry parsing and validation
- **mcp-apps-validator**: MCP Apps UI resource detection and validation
- **ucp-commerce-validator**: UCP (Universal Commerce Protocol, Google/Shopify) and ACP (Agentic Commerce Protocol, OpenAI/Stripe) validation — fetches `.well-known/ucp` and ACP feed/discovery, validates manifest structure, classifies commerce capabilities, and flags the MCP transport

## Experimental

{{experimental}}

- **ucp-prava-grader** *(hackathon experiment)*: "Payable" — a probabilistic categorization layer on top of the UCP-Commerce validator that maps each merchant to an MCC and grades it against Prava §8 (payable-via-Prava: `eligible` / `prohibited` / `not_assessed`). A standalone experiment, not pinned into the assessment engine.
